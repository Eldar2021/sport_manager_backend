Read all AI context documentation files in this project and summarize the key points:

1. Read `CLAUDE.md` (root) if it exists.
2. Read `GEMINI.md` if it exists.
3. Read `README.md` if it exists.
4. Read all files in `docs/` directory:
   - `docs/architecture.md`
   - `docs/api-docs.md`
   - `docs/code-rules.md`
   - `docs/contributing.md`
   - `docs/git-naming-convention.md`
5. Read `analysis_options.yaml` for lint rules.
6. Read `Makefile` for available commands.
7. Read root `pubspec.yaml` for workspace config and melos scripts.

After reading, provide a concise summary of the project context, key rules, and conventions that should be followed during development. This ensures you have full context for subsequent tasks.
